apiVersion: v1
kind: Service
metadata:
  name: expense-backend-service
spec:
  type: NodePort
  selector:
    app: expense-backend
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30002
